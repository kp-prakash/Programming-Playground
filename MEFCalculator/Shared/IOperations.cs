namespace Shared
{
    public interface IOperations
    {
        int Add(int number1, int number2);
        int Subtract(int number1, int number2);
        long Multiply(int number1, int number2);
        double Divide(int number1, int number2);
    }
}
